<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://fr.web.img4.acsta.net/r_1280_720/newsv7/19/09/26/10/19/3162437.jpg);">
  <div class="container">
    <h1>Find the most curated movies lists here!</h1>
  </div>
</div>
<div class="container">
  <h1>Top lists of the moment:</h1>
  <div class="lists-card">
      <% @lists.each do |list| %>
      <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path @list.img.key, height: 300, crop: :fill %>')">
        <%= link_to list.name, list_path(list) %>, <%= link_to "Delete", list_path(list), data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this bookmark?"}%>
      </div>
      <% end %>
    </ul>
  </div>
</div>

<%= link_to "Create a new list", new_list_path %>
